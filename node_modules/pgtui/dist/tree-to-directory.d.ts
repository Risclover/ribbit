import { DatabaseTree } from './types/tree.js';

declare const treeToDirectoryStructure: (db: DatabaseTree) => {
    [filePath: string]: string;
};
declare const treeToDirectory: (db: DatabaseTree, outputDir: string, { header }?: {
    header?: string;
}) => Promise<void>;

export { treeToDirectory as default, treeToDirectory, treeToDirectoryStructure };
